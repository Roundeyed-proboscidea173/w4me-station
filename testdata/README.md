# Test data

`oracles/` contains immutable replay inputs and expected WASM-4 state used by
the automated regression suite. These files are test fixtures, not generated
benchmark evidence.

Commands must write new logs, screenshots, profiles, and receipts under
`build/reports/`.
