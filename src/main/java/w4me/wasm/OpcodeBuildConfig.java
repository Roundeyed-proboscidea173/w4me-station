package w4me.wasm;

/** Compile-time opcode layout for the production artifact. */
final class OpcodeBuildConfig {
    static final boolean DENSE_OPCODE_DISPATCH = true;

    private OpcodeBuildConfig() {}
}
