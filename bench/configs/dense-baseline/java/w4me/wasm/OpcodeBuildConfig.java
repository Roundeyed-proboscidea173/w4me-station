package w4me.wasm;

/** Compile-time sparse-opcode baseline for isolated dense-dispatch A/B tests. */
final class OpcodeBuildConfig {
    static final boolean DENSE_OPCODE_DISPATCH = false;

    private OpcodeBuildConfig() {}
}
